#include <stdio.h>
#include <conio.h>
void main()
{
    float ratio=100.56;
    {
        float ratio;
        ratio=(float)(5+2)/(float)(5-3);
        printf("Ratio=%.2f",ratio);
    }
    printf("\n\n\nRatio=%.2f",ratio);
    getch();
}
