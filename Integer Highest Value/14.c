#include <stdio.h>
#include <conio.h>
void main()
{
    int positive_value=32767, negetive_value=-32768;
    printf("%d + 1 is %d", positive_value,positive_value+1);
    printf("\n%d - 1 is %d", negetive_value,negetive_value-1);
    getch();
}
